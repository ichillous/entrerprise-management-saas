spring:
  application:
    name: HusnaBackend
  datasource:
    url: jdbc:postgresql://localhost:5432/husnabackend
    username: admin@husna.app
    password: Mansoor#106Pizza%$#
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  reactor:
    debug-agent:
      enabled: true
app:
  jwtSecret: XyFY+oKSdIfj1eZuJYAmdG3RBNc87ZAXbOiqNl1tGR0=
  jwtExpirationInMs: 960000000
server:
  port: 8080
logging:
  level:
    root: INFO  # Keep root logging at INFO to avoid too much noise
    org.springframework.security: DEBUG  # Enable detailed logs for Spring Security
    org.springframework.web: DEBUG  # Enable detailed logs for Spring Web layer (controllers, etc.)
    app.husna.husnabackend: DEBUG  # Enable debug logs for your application package
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n"  # Clean and concise log pattern
